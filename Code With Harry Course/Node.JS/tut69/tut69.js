console.log("This is Tutorial 69");

// Commands used in NPM :
    // npm init [creates a package]
    // npm install slugify [to install slugify]
    // npm install/uninstall express [install and uninstall express]
    // npm install slugify@1.3.5 [to install specific version of slugify]
        //-> here 1.3.5 stands as major.minor.patch updates 
    // npm i browserify [i is used as shortcut for install]
    // "browserify": "^17.0.0" [^ this symbol is used so that the version will be constant]
    // "browserify": "~17.0.0"  [~This symbol is used so that the version gets updated]
    // "browserify": ">17.0.0"  [> This symbol is used so that the version gets updated in greater than     previous version]
    // npm view slugify version     [shows the version of slugify installed]